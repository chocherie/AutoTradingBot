/**
 * Vercel Blob pathname for the SQLite file uploaded by the Python bot after each run.
 * Keep in sync with app/api/admin/sync-db/route.ts
 */
export const DASHBOARD_BLOB_PATHNAME = "paper-trading/trading_bot.db";
